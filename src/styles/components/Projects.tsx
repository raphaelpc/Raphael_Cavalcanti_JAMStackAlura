import styled from 'styled-components';

const ProjectsContainer = styled.main`
  height: 100vh;

  color: ${props => props.theme.colors.background.main.contrastText};
`;

export default ProjectsContainer;
